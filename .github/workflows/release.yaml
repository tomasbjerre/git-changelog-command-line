name: Release

on:
  workflow_dispatch: {}

permissions:
  contents: write
  id-token: write

jobs:
  release:
    uses: tomasbjerre/.github/.github/workflows/bundle-gradle-command-line-release.yaml@master
    secrets: inherit
